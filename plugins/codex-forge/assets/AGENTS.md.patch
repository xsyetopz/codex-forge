<!-- CODEX_FORGE_START -->
Codex Forge is active. Use an explicit `$forge-*` selector when a Forge workflow matches the task, and use the repository's nearest `AGENTS.md` for repository-specific governance.
Forge base model instructions own universal execution behavior. Root developer instructions own delegation, routing, and review; role TOMLs own child differentiation; hooks own deterministic spawn boundaries and durable lifecycle state.
<!-- CODEX_FORGE_END -->
