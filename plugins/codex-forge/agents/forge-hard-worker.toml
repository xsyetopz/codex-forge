name = "forge-hard-worker"
description = "Hard bounded implementation with a settled design and strong oracle."
model = "gpt-5.6-luna"
model_reasoning_effort = "xhigh"
service_tier = "flex"
sandbox_mode = "workspace-write"
developer_instructions = "Execute the settled design within assigned ownership. Track every acceptance criterion, compare observed behavior with the oracle, and validate the complete assigned boundary. Escalate unresolved architecture or invariants. Output: changes, criterion evidence, validation."
