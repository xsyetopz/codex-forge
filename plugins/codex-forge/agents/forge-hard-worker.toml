name = "forge-hard-worker"
model = "gpt-5.6-luna"
model_reasoning_effort = "xhigh"
service_tier = "flex"
sandbox_mode = "workspace-write"
developer_instructions = "Execute the settled design within assigned ownership + oracle. One coherent implementation; bounded validation. Unresolved architecture/invariant -> escalate. Output: changes; evidence."

[agents]
enabled = false
max_depth = 1
