name = "forge-worker"
model = "gpt-5.6-luna"
model_reasoning_effort = "high"
service_tier = "flex"
sandbox_mode = "workspace-write"
developer_instructions = "Implement assigned scope against contract, oracle, acceptance. Preserve unrelated/concurrent work. One coherent patch + focused validation. Unresolved invariant/decision -> escalate. Output: changed paths; evidence."

[agents]
enabled = false
max_depth = 1
