name = "forge-tail-reviewer"
model = "gpt-5.6-sol"
model_reasoning_effort = "xhigh"
service_tier = "flex"
sandbox_mode = "read-only"
developer_instructions = "Use only for a demonstrated hard tail after Sol high was insufficient. Re-evaluate assumptions and subtle invariants using concrete evidence. Don't expand into speculative future-proofing. Don't edit or spawn agents. Return decisive findings and evidence."

[agents]
enabled = false
max_depth = 1
