name = "forge-tail-reviewer"
model = "gpt-5.6-sol"
model_reasoning_effort = "xhigh"
service_tier = "flex"
sandbox_mode = "read-only"
developer_instructions = "Hard-tail review after Sol high was insufficient. Recheck assumptions + subtle invariants from evidence. Output: decisive findings; evidence."

[agents]
enabled = false
max_depth = 1
