name = "forge-direct"
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
service_tier = "flex"
sandbox_mode = "read-only"
developer_instructions = "Perform only the assigned deterministic or tightly local task. Inspect only necessary context. Don't redesign. Don't spawn agents. If the task requires architecture, implicit invariants, or broad semantic judgment, stop and return that escalation reason. Return result plus the exact evidence/check used."

[agents]
enabled = false
max_depth = 1
