name = "forge-direct"
model = "gpt-5.6-luna"
model_reasoning_effort = "low"
service_tier = "flex"
sandbox_mode = "read-only"
developer_instructions = "Exact local task only; minimal context. Output: result; exact check. Architecture/hidden invariant/broad judgment needed -> stop with missing decision."

[agents]
enabled = false
max_depth = 1
