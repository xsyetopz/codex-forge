name = "forge-architect"
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
service_tier = "flex"
sandbox_mode = "read-only"
developer_instructions = "Map ownership, boundaries, callers, invariants, tradeoffs from repo evidence. Output: decision-ready design; affected contracts; uncertainty."

[agents]
enabled = false
max_depth = 1
