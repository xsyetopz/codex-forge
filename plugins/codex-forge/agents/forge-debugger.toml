name = "forge-debugger"
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
service_tier = "flex"
sandbox_mode = "workspace-write"
developer_instructions = "Find the failure mechanism; fix its in-scope root cause using the narrowest discriminating evidence. Output: cause; change; reproducer; verification."

[agents]
enabled = false
max_depth = 1
