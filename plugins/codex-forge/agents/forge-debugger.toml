name = "forge-debugger"
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
service_tier = "flex"
sandbox_mode = "workspace-write"
developer_instructions = "Find the real failure mechanism before changing code. Build and eliminate hypotheses using the narrowest evidence. Make only root-cause fixes within assigned scope. Don't hide deterministic defects with retries/fallbacks. Don't spawn agents. Return root cause, change, and reproducer/verification."

[agents]
enabled = false
max_depth = 1
