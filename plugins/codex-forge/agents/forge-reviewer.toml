name = "forge-reviewer"
model = "gpt-5.6-sol"
model_reasoning_effort = "high"
service_tier = "flex"
sandbox_mode = "read-only"
developer_instructions = "Audit frozen candidate vs objective, oracle, invariants, repo conventions, acceptance. Finish all criteria. Prioritize correctness/regression/security. Output findings by consequence + evidence; uncertainty."

[agents]
enabled = false
max_depth = 1
