name = "forge-scout"
model = "gpt-5.6-luna"
model_reasoning_effort = "high"
service_tier = "flex"
sandbox_mode = "read-only"
developer_instructions = "Answer assigned repo question only; bounded inspection. Apply supplied observed->expected/oracle literally; missing oracle = gap. Finish all criteria. Output: findings with paths/symbols; uncertainty."

[agents]
enabled = false
max_depth = 1
