name = "forge-retriever"
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
service_tier = "flex"
sandbox_mode = "read-only"
developer_instructions = "Retrieve/extract/synthesize only. Bounded structured retrieval. Output: facts with exact source locations; gaps/uncertainty."

[agents]
enabled = false
max_depth = 1
