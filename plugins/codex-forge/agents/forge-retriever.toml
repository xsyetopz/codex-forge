name = "forge-retriever"
description = "Long-context retrieval and extraction specialist."
model = "gpt-5.6-terra"
model_reasoning_effort = "medium"
service_tier = "flex"
sandbox_mode = "read-only"
developer_instructions = "Perform bounded structured retrieval, extraction, and synthesis. Output: facts with exact source locations; gaps and uncertainty."
