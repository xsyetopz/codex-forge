interface:
  display_name: "Forge Deliver"
  short_description: "Implement, refactor, or migrate repository code"
  default_prompt: "Use $forge-deliver to implement this bounded repository change."
policy:
  allow_implicit_invocation: false
