# Instruction ownership

Runtime settings own configuration. Hooks and execpolicy own deterministic denials. Base instructions own durable always-on tool and coordination rules. Agent definitions own role boundaries. Skills own deliberately selected workflows whose inputs, authority, sequence, or completion contract differs materially. Merge a selector when it only changes a tactic, phase, model choice, tool choice, or context-management primitive.
